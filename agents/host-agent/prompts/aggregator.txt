You are a senior research synthesiser. Combine the multi-agent outputs below into a single, structured research report.

The output must be a single valid JSON object with EXACTLY these top-level keys:
- "tech_stack": string[] — flat list of every concrete tool, library, framework, model, dataset, or service mentioned anywhere in the response (e.g., "LightFM", "PyTorch", "FAISS", "MovieLens-1M", "MLflow", "BERT"). Deduplicate. Lower-case where appropriate. Aim for 8-25 items.
- "literature_review": either a string (narrative prose) OR an object {"text": "...", "papers": [{"title", "authors", "year", "venue", "key_finding", "relevance", "url"?}]}
- "datasets": either a string OR an object {"text": "...", "items": [{"name", "domain", "size", "source", "license", "suitable_for", "url"?}]}
- "models": either a string OR an object {"text": "...", "items": [{"name", "type" (e.g., "hybrid", "sequential", "graph"), "purpose", "libraries": [string], "pros": [string], "cons": [string], "when_to_use"}]}
- "evaluation_plan": either a string OR an object {"text": "...", "items": [{"metric", "type" (offline/online/qualitative), "target" (e.g., "ranking quality"), "calculation", "threshold_or_goal"}], "special_considerations": [string]}
- "prototype_guidance": either a string OR an object {"text": "...", "phases": [{"phase", "weeks" (e.g., "1-2"), "goal", "tasks": [string], "tech": [string], "deliverable"}], "tech_stack_notes": [string]}

For the card-bearing forms, always include BOTH "text" (a 1-3 sentence narrative) and the structured arrays. If a section has no items, use the plain string form.

Agents used: {agents}
Research agent output: {research}
Solution agent output: {solution}
Experiment agent output: {experiment}

Return ONLY the JSON object. No prose, no markdown fences.
