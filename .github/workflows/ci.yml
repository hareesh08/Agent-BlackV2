#name: Build & Push Docker Images
#
#on:
#  push:
#    branches: [main, master]
#    tags: ['v*']
#  pull_request:
#    branches: [main, master]
#
#env:
#  REGISTRY: ghcr.io
#  IMAGE_BASE: ghcr.io/${{ github.repository_owner }}/agent-black
#  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
#
#permissions:
#  contents: read
#  packages: write
#
#concurrency:
#  group: ${{ github.workflow }}-${{ github.ref }}
#  cancel-in-progress: true
#
#jobs:
#  build-push:
#    runs-on: ubuntu-latest
#    strategy:
#      fail-fast: false
#      matrix:
#        include:
#          - service: research-agent
#            dockerfile: agents/research-agent/Dockerfile
#          - service: solution-agent
#            dockerfile: agents/solution-agent/Dockerfile
#          - service: experiment-agent
#            dockerfile: agents/experiment-agent/Dockerfile
#          - service: control-panel
#            dockerfile: agents/host-agent/Dockerfile.control-panel
#          - service: frontend
#            dockerfile: devops/Dockerfile.frontend
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v3
#
#      - name: Login to GHCR
#        if: github.event_name != 'pull_request'
#        uses: docker/login-action@v3
#        with:
#          registry: ${{ env.REGISTRY }}
#          username: ${{ github.actor }}
#          password: ${{ secrets.GITHUB_TOKEN }}
#
#      - name: Docker meta
#        id: meta
#        uses: docker/metadata-action@v5
#        with:
#          images: ${{ env.IMAGE_BASE }}/${{ matrix.service }}
#          tags: |
#            type=ref,event=branch
#            type=ref,event=tag
#            type=sha,prefix=
#            type=raw,value=latest,enable={{is_default_branch}}
#
#      - name: Build & Push
#        uses: docker/build-push-action@v6
#        with:
#          context: .
#          file: ${{ matrix.dockerfile }}
#          push: ${{ github.event_name != 'pull_request' }}
#          tags: ${{ steps.meta.outputs.tags }}
#          labels: ${{ steps.meta.outputs.labels }}
#          cache-from: type=gha
#          cache-to: type=gha,mode=max
#          platforms: linux/amd64
#
#  notify:
#    runs-on: ubuntu-latest
#    needs: build-push
#    if: github.event_name != 'pull_request' && success()
#    steps:
#      - name: Summary
#        run: |
#          echo "## All images built and pushed to GHCR" >> $GITHUB_STEP_SUMMARY
#          echo "" >> $GITHUB_STEP_SUMMARY
#          echo "Deploy on your VPS with:" >> $GITHUB_STEP_SUMMARY
#          echo '```bash' >> $GITHUB_STEP_SUMMARY
#          echo "curl -sSL https://raw.githubusercontent.com/${{ github.repository }}/main/deploy.sh | bash" >> $GITHUB_STEP_SUMMARY
#          echo '```' >> $GITHUB_STEP_SUMMARY
